# Auth.jsがコールバックURLやリダイレクトURLを生成する際のベースURL
# 本番環境では https://your-domain.com のように設定する
NEXTAUTH_URL="http://localhost:3000"

# Auth.jsにホスト名の検証をスキップさせ、プロキシ経由のリクエストを信頼する
# true にすると X-Forwarded-Host ヘッダーを信頼し、NEXTAUTH_URL の厳密なチェックを緩和する
# 本番環境でリバースプロキシやロードバランサーを使う場合に必要
# セキュリティ上、信頼できる環境でのみ true にすること
AUTH_TRUST_HOST="true"
